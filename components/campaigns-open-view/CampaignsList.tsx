import { Text, View } from "../Themed";

const CampaignsList: React.FC = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      <Text>Please open web version to see the list</Text>
    </View>
  );
};

export default CampaignsList;
