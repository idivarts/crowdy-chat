import { Text, View } from "../Themed";

const CampaignsBoard = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Please open web version to see the board</Text>
    </View>
  );
};

export default CampaignsBoard;
