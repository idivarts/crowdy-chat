import AppLayout from "@/layouts/app-layout";

import { Text, View } from "@/components/Themed";

const Login = () => {
  return (
    <AppLayout>
      <View>
        <Text>Login</Text>
      </View>
    </AppLayout>
  );
};

export default Login;
